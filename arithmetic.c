#include <stdio.h>

int main() {

    int x = 10;
    int y = 3;
    int z = 0;

    //z = x + y;
    //z = x - y;
    //z = x * y;
    //z = x / y;
    //z = x % y;

    //x--;
    x += 3;

    //printf("The sum of x and y is %d\n", z);
    printf(" %d\n", x);

    return 0;
}